# The build writes to build/dist/, which the line above already covers. The dist/ directory in
# the source tree is NOT build output: it holds fix_enhancers.ini, the shipped configuration,
# and ignoring it would leave the repository without the file every install needs.
build/
release/*
!release/.gitignore
