# engine

A clean-room reimplementation of the Riot Engine subsystems, so the game can
eventually run without the 2002 binary.

**Not started.** `runtime/` comes first: it is what makes the game playable
today, and it is also how the engine's behaviour gets documented precisely
enough to reimplement.
