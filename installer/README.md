# installer

Packaging for a one-click install: the loader, the plugin folder and the
configuration file, placed correctly beside a retail installation.

**Not started.** It will need to cope with an install that already has a
`d3d8.dll` wrapper and the community `Fellowship.dll` patcher present, since
that is the common case and both must keep working.
